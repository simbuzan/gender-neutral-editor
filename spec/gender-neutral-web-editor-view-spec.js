'use babel';

import GenderNeutralWebEditorView from '../lib/gender-neutral-web-editor-view';

describe('GenderNeutralWebEditorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
